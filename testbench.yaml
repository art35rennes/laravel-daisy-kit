laravel: '@testbench'

providers:
  - Art35rennes\DaisyKit\DaisyKitServiceProvider

workbench:
  start: '/'
  install: true
  discovers:
    config: true
    web: true
    views: true
  build:
    - create-sqlite-db
  sync:
    - from: storage
      to: workbench/storage
      reverse: true

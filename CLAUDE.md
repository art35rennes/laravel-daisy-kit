# Claude Code

Follow `AGENTS.md`.

When a task touches Blade UI, layouts, templates, or package frontend behavior:

- Prefer existing Daisy Kit aliases before creating new markup.
- Consult `resources/boost/skills/daisy-kit-component-reuse/references/component-catalog.md`.
- Use host overrides under `resources/views/vendor/daisy/...` before duplicating package views.
